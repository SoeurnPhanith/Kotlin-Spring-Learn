package org.example.class_object

/*

    + Visibility Modifiers in Kotlin having 4 type :

        1. public (default) : everywhere class, file, package in project

        2. private : can access only in same class

        3. protected : can access in same class and child class

        4. internal : can use in same class, child class, different file , in module but not everywhere

*/

/// Note  : module > package
